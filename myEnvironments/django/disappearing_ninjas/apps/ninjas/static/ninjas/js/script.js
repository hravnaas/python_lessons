$(document).ready(function(){
	// Read the src attribute and append the fileName attribute.
	// Store the concatenated string in the src attribute.
    // $('img').attr('src', $('img').attr('src') + $('img').attr('fileName'));
    // $('img').attr('hidden', false);
});